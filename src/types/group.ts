export interface Group {
  _id: string;
  groupName: string;
  categorySelect: string;
  meetingLocation: string;
  maxMembers: number;
  startDate: string;
  imageURL: string;
  username: string;
  email: string;
}
